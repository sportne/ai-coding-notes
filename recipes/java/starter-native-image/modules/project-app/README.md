# App Module

Add the application entrypoint here when real behavior exists. Replace `<main-class>` in `build-logic/src/main/groovy/java-application-conventions.gradle` before running Native Image tasks.
